# Backend .env for local development (outside Docker)
# In Docker, these are set via docker-compose.yml environment section
CKPT_PATH=outputs/checkpoints/best_cxr_multilabel_3.pt
VAL_LOADER_PATH=outputs/val_samples.pkl
TEMP_DIR=backend/static/heatmaps
